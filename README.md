# rename

PowerShell scripts for safely transliterating Cyrillic filenames and replacing spaces with underscores in the current folder.

Run `rename_new.ps1` from the folder whose files should be renamed.
